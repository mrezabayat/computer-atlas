---
title: Database Foundations
summary: The ideas every working programmer should know about relational databases — model, query language, design, indexing, transactions, and the NoSQL alternatives.
audience: intermediate
topics:
  - ref: database
    section: The model
  - ref: relational-model
    section: The model
  - ref: normalization
    section: The model
  - ref: sql
    section: The language
  - ref: query-plan
    section: The language
    optional: true
  - ref: orm
    section: The language
    optional: true
  - ref: indexing
    section: Performance and reliability
  - ref: transaction-acid
    section: Performance and reliability
  - ref: schema-migration
    section: Performance and reliability
    optional: true
  - ref: nosql
    section: Beyond relational
  - ref: key-value-store
    section: Beyond relational
    optional: true
  - ref: document-store
    section: Beyond relational
    optional: true
  - ref: etl
    section: Data pipelines
    optional: true
  - ref: data-warehouse
    section: Data pipelines
    optional: true
updated: 2026-06-08
---

Most applications stand or fall on how well they use their database. This short path walks through the relational model (the math), SQL (the language), normalisation (the design discipline), indexing (the performance lever), and ACID transactions (the consistency guarantee).

It then covers the NoSQL landscape and the data pipeline tools that sit above raw storage, so you know what to reach for when the relational model isn't the right fit.
